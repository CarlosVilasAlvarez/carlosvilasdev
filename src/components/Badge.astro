---
interface Props {
  text: string;
  class: string;
}

const { text, class: className } = Astro.props;
---

<span
  class={`inline-block sbg-blue-100 text-black-800 text-xs font-medium me-2 px-2.5 py-0.5 rounded-sm bg-orange-200 text-gray-800 ${className}`}
  >{text}</span
>
